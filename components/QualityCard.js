import React from "react";
import { Text, View } from "react-native";
// import { Card } from "react-native-elements";

const QualityCard = ({ quality }) => {
  return <Text>Qualités du produit scanné</Text>;
};
export default QualityCard;
